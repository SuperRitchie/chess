# chess + utils
python-chess==1.999
requests==2.32.5

# ML stack (aligned so TF-JS + TF-DF work together)
tensorflow==2.20.0
tensorflow-decision-forests==1.12.0
tensorflowjs==4.20.0

# Numpy compatible with TF 2.19 on Py3.11
numpy==1.26.4
